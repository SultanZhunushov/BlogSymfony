{% extends 'base.html.twig' %}

{% block body %}
<h1>Inserisci/Modifica un Blog</h1>
<div class="container">
    <div class="row justify-content-md-center">
        <div class="col">
            {{ form_start(form) }}
        </div>
        <div class="col">
            {{ form_rest(form) }}
            <input type="submit" class="btn btn-primary"/>
        
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
